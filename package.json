{
    "name": "mern-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "cd backend && npm install && npm start",
      "build": "cd frontend && npm install && npm run build"
    }
  }
  